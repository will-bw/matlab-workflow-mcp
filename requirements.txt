# MATLAB MCP Server v3.0 依赖
# ============================
# 安装: pip install -r requirements.txt
#
# v3.0 不再需要 MATLAB Engine API！
# 仅需确保 matlab 可执行文件在 PATH 中（或设置 MATLAB_EXE 环境变量）。
#
# Python 版本要求: 3.9+

# MCP SDK (Streamable HTTP + SSE)
mcp[cli]>=1.10.0

# ASGI 服务器
uvicorn>=0.27.0

# HTTP 工具库
httpx>=0.25.0

# SSE 向后兼容
sse-starlette>=1.6.0

# 异步支持
anyio>=4.0.0

# Starlette 路由
starlette>=0.37.0

# 系统资源监控 (CPU/内存/磁盘)
psutil>=5.9.0
